# Remember, leave a key empty if there is no value.  None will be a string,
# not a Python "NoneType"
client:
  hosts:
    - 172.16.10.223
    - 172.16.10.224
    - 172.16.10.225
  port: 9200
  url_prefix:
  use_ssl: False
  certificate:
  client_cert:
  client_key:
  ssl_no_validate: False
  http_auth:
  timeout: 3600
  master_only: False

logging:
#  loglevel: DEBUG
  loglevel: INFO
#  logfile: /home/luohuanfeng/curator.log
  logfile:
  logformat: default
  blacklist: ['elasticsearch', 'urllib3']