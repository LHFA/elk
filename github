 Command line instructions
Git global setup

git config --global user.name "罗宦锋"
git config --global user.email "luohuanfeng@ubox.cn"

Create a new repository

git clone http://git.uboxol.com/devops/ELK_Stack.git
cd ELK_Stack
touch README.md
git add README.md
git commit -m "add README"
git push -u origin master

Existing folder

cd existing_folder
git init
git remote add origin http://git.uboxol.com/devops/ELK_Stack.git
git add .
git commit -m "Initial commit"
git push -u origin master

Existing Git repository

cd existing_repo
git remote add origin http://git.uboxol.com/devops/ELK_Stack.git
git push -u origin --all
git push -u origin --tags